class Tour {
  String title;
  double rate;
  double price;
  String imageAddress;
  String type;
  String numberOfTours;
  int numberOfPersons;

  Tour(this.title, this.rate, this.price, this.imageAddress, this.type,
      this.numberOfTours, this.numberOfPersons);
}
